package 쓰레드예제6번;
// Thread 의 안전한 종료법은 stop 플래그 구현 방법고 interrupt() 메소드를 사용하는 방벙이 있다.
public class ThreadEx6 {
    public static void main(String[] args) {
        InterruptThread interruptThread = new InterruptThread();
        //intThread.start();

    }
}
